import { AsSortimentCategoryPipe } from './as-sortiment-category.pipe';

describe('AsSortimentCategoryPipe', () => {
	it('create an instance', () => {
		const pipe = new AsSortimentCategoryPipe();
		expect(pipe).toBeTruthy();
	});
});
