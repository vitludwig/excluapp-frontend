import { BackBtnDirective } from './back-btn.directive';

describe('BackBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new BackBtnDirective();
    expect(directive).toBeTruthy();
  });
});
