import { DistinctFromPipe } from './distinct-from.pipe';

describe('DistinctFromPipe', () => {
  it('create an instance', () => {
    const pipe = new DistinctFromPipe();
    expect(pipe).toBeTruthy();
  });
});
